version: 0.2

phases:
  pre_build:
    commands:
      - npm install
      - npm install -g @angular/cli@11.2.3
  build:
    commands:
      - npm run build
artifacts:
  files:
     - '**/*'
  discard-paths: no
  base-directory: build
